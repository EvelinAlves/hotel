    
    /*if(!elementMenu.classList.contains("menu-negativo")){
        if(elementMenu.getBoundingClientRect().top < 0) {
            elementMenu.classList.add('menu-negativo');
            elementMenu.classList.remove('menu-positivo');
            console.log('colocou fixo, pois fixo < 0 e não tinha fixo', !elementMenu.classList.contains("fixo"), elementMenu.getBoundingClientRect().top);
        }
    }

    if(elementMenu.classList.contains("menu-positivo")){
        if(elementMenu.getBoundingClientRect().top > 0) {
            elementMenu.classList.remove('menu-negativo');
            elementMenu.classList.add('menu-positivo');
            console.log('removeu fixo, pois fixo > 0 e tinha fixo', elementMenu.classList.contains("fixo"), elementMenu.getBoundingClientRect().top);
        }
    }*/

    if(!elementMenu.classList.contains("fixo")){
        if (elementMenu.getBoundingClientRect().top < 0){
            elementMenu.classList.add('fixo');
            console.log('while 1');
        }
    }
    if(elementMenu.classList.contains("fixo")){
        if (elementMenu.getBoundingClientRect().top >= 0){
            elementMenu.classList.remove('fixo');
            console.log('while 2');
        }
    }

    // while (elementMenu.getBoundingClientRect().top < 0){
    //     if(!elementMenu.classList.contains("fixo")){
    //         elementMenu.classList.add('fixo');
    //         console.log('while 1');
    //         break;
    //     }
    // }
    // while (elementMenu.getBoundingClientRect().top > 0){
    //     if(elementMenu.classList.contains("fixo")){
    //         elementMenu.classList.remove('fixo');
    //         console.log('while 2');
    //         break;
    //     }
    // }


    // if (elementMenu.getBoundingClientRect().top < 0 && !elementMenu.classList.contains("fixo")){
    //     
    // } else {
    //     elementMenu.classList.remove('fixo');
    // }





function ativaNoScroll() {
    elementMenu = document.querySelector('#menu');

    if(elementMenu.getBoundingClientRect().top < 0 && !elementMenu.classList.contains("fixo")) {
        elementMenu.classList.add('fixo');
        console.log("colocou");
    }
    if(elementMenu.getBoundingClientRect().top > 0) {
        elementMenu.classList.remove('fixo');
        console.log("removeu");
    }
}

// window.addEventListener.setInterval(('scroll', ativaNoScroll), 100);
window.setInterval(window.addEventListener('scroll', function() {
    elementMenu = document.querySelector('#menu');

    if(elementMenu.getBoundingClientRect().top < 0 && !elementMenu.classList.contains("fixo")) {
        elementMenu.classList.add('fixo');
        console.log("colocou");
    } else
    if(elementMenu.getBoundingClientRect().top >= 0) {
        elementMenu.classList.remove('fixo');
        console.log("removeu");
    }
    console.log('a');
}), 100);

